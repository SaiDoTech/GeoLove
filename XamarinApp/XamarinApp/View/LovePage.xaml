<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.View.LovePage">
    <ContentPage.Content>
        <StackLayout x:Name="loveStack">

            <Label x:Name="profileLabel" 
                Text="Swipe"
                FontSize="Title"
                Padding="0, 10, 0, 10"
                VerticalTextAlignment="Center"
                HorizontalTextAlignment="Center" />

            <SwipeView x:Name="swipeView"
                       VerticalOptions="FillAndExpand">
                <Frame x:Name="userFrame"
                    Margin = "10,10,10,10"
                    Padding = "50,50,50,50"/>
            </SwipeView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>